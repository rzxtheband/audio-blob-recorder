{
  "name": "netlify-serverless-audio-app",
  "version": "1.0.0",
  "description": "A Node.js application to handle audio uploads and retrievals from IBM Cloud Object Storage using Netlify serverless functions.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Nothing to build' && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.318.0",  // The specific version might vary
    "formidable": "^2.1.1"              // Ensure to install a compatible version
  },
  "devDependencies": {},
  "author": "Siddharth Mishra",
  "license": "MIT"
}
